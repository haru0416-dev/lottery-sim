version: 2
updates:
  # GitHub Actions の更新(checkout/setup-bun のランタイム非推奨などを自動追従)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
